import React, { PropTypes } from 'react';
import {
  Text,
  View
} from 'react-native';

const Hello = React.createClass({
  render () {
    return (
      <Text>Hello</Text>
    )
  }
})

export default Hello;
